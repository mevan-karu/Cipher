1. create table grade_point (Grade varchar(2) primary key, Point numeric(2,1) check(Point>=0)); 2. insert into grade_point values ('A+',4.2), ('A',4.0),('A-',3.7),('B+',3.5),('B',3.0),('B-',2.7),('C+',2.3),('C',2.0),('C-',1.5),('D',1.0);3. select student.name,sum(Point) as sum_of_gp from student left outer join (takes left outer join grade_point using(grade) ) using(ID) group by student.ID order by sum_of_gp DESC;4. DELIMITER $$	create function num_of_student(course_id varchar(8)) 	returns integer     	begin    	declare student_count integer;    	select count(*) into student_count    	from takes    	where takes.course_id=course_id;    	return student_count;    	end $$	DELIMITER ;5. select course_id from course where num_of_student(course_id)>5 order by course_id;6. DELIMITER $$   create trigger grade_check before insert on takes   for each row begin	if (NEW.grade not in (select grade from grade_point)) then		set NEW.grade = null;	end if;   end $$   DELIMITER ;7. create view faculty as (select ID,name,dept_name from instructor);8. create user "uomcse" identified by "uomcse123";9. grant select on faculty to "uomcse";10. grant all privileges on takes to "uomcse";